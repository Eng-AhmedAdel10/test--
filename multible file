function sub(obj) {
    var files = obj.files;
    var fileName =[];
    for(i=0 ; i < files.length ; i++)
    {
        fileName.push(files[i].name)
    }
    document.getElementById("yourBtn").innerHTML = fileName.join(", ");
    console.log(fileName);
  }
